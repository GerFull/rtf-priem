import React from 'react'
import MainInfo from '../../components/mainInfo/mainInfo'

interface Props { }

function Home(props: Props) {
   const { } = props

   return (
      <>
         <MainInfo />
      </>
   )
}

export default Home
